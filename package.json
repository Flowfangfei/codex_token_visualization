{
  "name": "codex-usage-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Local Codex usage exporter and dashboard.",
  "scripts": {
    "start": "node server.js",
    "export": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/export-daily.ps1",
    "task:register": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/register-daily-task.ps1"
  },
  "engines": {
    "node": ">=22"
  }
}
